import { PwUppercasePipe } from './pw-uppercase.pipe';

describe('PwUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new PwUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
